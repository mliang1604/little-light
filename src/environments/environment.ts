// Production Bungie app credentials (https://www.bungie.net/en/Application).
// These are not secrets: API keys and public-client ids ship in the JS bundle
// and are locked to the registered Origin Header.
export const environment = {
  bungieApiKey: '',
  bungieClientId: '',
};
