// Local-dev Bungie app credentials (https://www.bungie.net/en/Application).
// Register a separate app for dev with redirect https://localhost:4200/auth/callback.
export const environment = {
  bungieApiKey: '',
  bungieClientId: '',
};
